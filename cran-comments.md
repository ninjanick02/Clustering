# CRAN Submission Comments

## Test environments

* local: R 4.3.0 (platform TBD)
* GitHub Actions:
  - macOS-latest (release)
  - windows-latest (release)  
  - ubuntu-latest (devel)
  - ubuntu-latest (release)
  - ubuntu-latest (oldrel-1)

## R CMD check results

There were no ERRORs, WARNINGs or NOTEs.

## Downstream dependencies

There are currently no downstream dependencies for this package.

## Submission Notes

This is the initial submission of the Clustering package.

The package provides efficient k-means clustering implementations in both R and C++ (via Rcpp).
